name: autoclaimexpense
app:
  runtime: dockerfile
  dockerfile: Dockerfile
  env:
    GOOGLE_API_KEY: ${GOOGLE_API_KEY}
  healthcheck:
    path: /serverCheck
    interval: 10
    timeout: 5
    retries: 5
